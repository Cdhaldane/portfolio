# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
/server/node_modules
/game/node_modules
/pipeline/node_modules

# testing
/coverage

# production
/build
/game/dist

# art sources and vendored CC0 packs (GALLOWS_HYMN.md Appendix B, §18.4).
# The ledger + lockfile are committed; the bytes are not.
/art

# Optimized asset output (§17.8). Rebuildable from art/ via npm run art.
/game/public/assets

# Pipeline scratch: de-lit/locked intermediates and the texture hash cache.
/pipeline/.work

# misc
.DS_Store
.env
.env.*
!.env.example
server/.env
.vercel

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
.env*
